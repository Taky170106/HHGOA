import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* screen-recorded demo at 1920x1080 — no dev overlay badge over the UI */
  devIndicators: false,
};

export default nextConfig;
